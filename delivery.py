"""Пункты выдачи заказов сторонних служб доставки.

Адреса ниже — реальные существующие пункты выдачи (найдены через открытые
справочники служб доставки), но это стартовый ограниченный набор для
Москвы, а не полный список филиалов служб (у каждой сети в реальности
тысячи точек по всей стране). Чтобы добавить другой город — допишите
список адресов в соответствующий словарь ниже, формат тот же.

Здесь нет обращений к внешним API: у СДЭК, Boxberry, Яндекс Маркета и
5Post «боевой» доступ к API выдаётся только по договору после регистрации
юрлица/самозанятого — это отдельный шаг, который делает сам владелец
магазина, а не что-то, что можно встроить в код без реальных учётных
данных.
"""

PICKUP_PROVIDERS = {
    'cdek': 'СДЭК',
    '5post': '5Post',
    'yandex_market': 'Яндекс Маркет',
    'boxberry': 'Boxberry',
}

PICKUP_POINTS = {
    'cdek': {
        'Москва': [
            'ул. Международная, 15',
            'ул. Кировоградская, 9, корп. 1 (постамат)',
            'ул. Производственная, 10, корп. 1',
        ],
    },
    '5post': {
        'Москва': [
            'ул. Новинки, 1 (пункт в «Пятёрочке»)',
            'Совхозная ул., 8Б (пункт в «Пятёрочке»)',
            'Радужная ул., 19а, стр. 1 (пункт в «Пятёрочке»)',
        ],
    },
    'yandex_market': {
        'Москва': [
            '1-я Тверская-Ямская ул., 25',
            'Неглинная ул., 20',
            'ул. Большая Полянка, 28, корп. 1',
            'Новинский бул., 8',
            'Пятницкое ш., 14',
        ],
    },
    'boxberry': {
        'Москва': [
            'Варшавское ш., 18, корп. 2 (ПВЗ «Москва Варшавское»)',
            'Рязанский пр-т, 2/1, корп. 5У (ПВЗ «Москва Рязанский»)',
            'ул. Профсоюзная, 118 (ПВЗ «Москва Профсоюзная»)',
            'ул. Плющиха, 42 (ПВЗ «Москва Плющиха»)',
        ],
    },
}


def get_providers():
    return PICKUP_PROVIDERS


def get_cities(provider):
    return sorted(PICKUP_POINTS.get(provider, {}).keys())


def get_points(provider, city):
    return PICKUP_POINTS.get(provider, {}).get(city, [])


def format_pickup_address(provider, city, point):
    """Собирает финальную строку адреса, которая сохраняется в orders.address —
    остальной код (админка, страница заказа) её просто показывает как есть."""
    provider_name = PICKUP_PROVIDERS.get(provider, provider)
    return f'Пункт выдачи «{provider_name}»: {point}, {city}'
